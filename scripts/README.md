# scripts/ — deployment helpers (optional)

| File | What it does |
|------|----------------|
| **`push-openai-key-to-vercel.ps1`** | Reads `OPENAI_API_KEY` from `demo/realtime-sales-demo/server/.env` and uploads it to a Vercel project (requires [Vercel CLI](https://vercel.com/docs/cli) and `vercel login`). |

For full go-live steps, use the repo root **`GO-LIVE-VERCEL.md`**.
