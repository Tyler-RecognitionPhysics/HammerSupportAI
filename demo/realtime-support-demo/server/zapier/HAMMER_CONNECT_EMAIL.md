# Zapier — Hammer Connect agreement email

Same Catch Hook and Gmail setup as MarketPoster (`agreementEmailHtml`, HTML mode). Filter on `event` = `agreement_email_request` only.

## Product detection

`selected_plan` contains **Hammer Connect** (standalone — not MarketPoster). Examples: `Hammer Connect`, `Hammer Connect standalone`.

`productLine`: `hammer_connect`

## Pricing

**$99/month** (USD) — fixed price; no tiers or overrides.

## Copy vs MarketPoster

Identical layout, spacing, and bolding as MarketPoster except **no** “Additional Users $50 monthly” line. Differences:

| Field | Hammer Connect |
|--------|----------------|
| Service description | **Hammer Connect** |
| Welcome / charge line brand | **Hammer Connect** |
| Thank you | **Hammer/Hammer Connect** |
| Subscription line | `$99/month` (no `+ N Users`) |
